<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Doenças</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container-table">
        <a href="/" class="voltar">← Voltar</a>
        <h1>Lista de Doenças possíveis</h1>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Pontuação</th>
            </tr>
        </thead>
        <tbody>
            <% result.forEach((disease) => { %>
                <tr>
                    <td><%= disease.id %></td>
                    <td><%= disease.nome %></td>
                    <td><%= disease.pontuacao %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
        <div class="pagination">
            <% if (currentPage > 1) { %>
                <a href="/resultSintomas?page=<%= currentPage - 1 %>">Anterior</a>
            <% } %>

            <% for (let i = 1; i <= totalPages; i++) { %>
                <a href="/resultSintomas?page=<%= i %>" 
                class="<%= i == currentPage ? 'active' : '' %>">
                <%= i %>
                </a>
            <% } %>

            <% if (currentPage < totalPages) { %>
                <a href="/resultSintomas?page=<%= currentPage + 1 %>">Próxima</a>
            <% } %>
        </div>
    </div>
</body>
</html>
